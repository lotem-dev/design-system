Context
You are working with an existing Design System that is already in production.
The Design System file is locked and must NOT be modified in any way.

Critical rules (must follow)

❌ Do NOT edit, rename, detach, or override any components, styles, or tokens in the Design System file

❌ Do NOT create new design tokens

❌ Do NOT change existing typography, colors, spacing, or radius values

✅ You may ONLY use existing components and presets as-is

✅ All work must be done in the App / Playground file only
